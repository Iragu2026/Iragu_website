# Production: set to your API custom domain, e.g. https://api.iraguforher.com
# IMPORTANT: Use the custom domain (NOT the .onrender.com URL) so the frontend
# and API share the same parent domain for cookie-based authentication.
# Local dev: leave empty — Vite proxy will forward /api requests to localhost.
VITE_API_BASE_URL=
